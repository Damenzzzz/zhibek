# env
.env
.env.local

# python
__pycache__/
*.pyc
.venv/
venv/

# vercel CLI project link (local-only, used to deploy the monorepo root so
# the project's Root Directory=apps/web setting resolves the same way the
# GitHub integration does)
/.vercel

# stray root-level node_modules (not part of any workspace here — apps/web
# has its own package.json/node_modules; this one accidentally ended up in
# the repo root and is too large to push to GitHub anyway, see supabase.exe)
/node_modules/
/package.json
/package-lock.json

# data (raw source collages are not committed — large, only needed locally
# for scripts/process_photos.py). data/catalog IS committed: it's the small
# (~KB-MB) processed output (cropped photos + items.db) that the deployed
# app needs to read at runtime, since Vercel serverless functions can't
# write/persist a local file the way a normal server can.
/data/raw/*
!/data/raw/.gitkeep
